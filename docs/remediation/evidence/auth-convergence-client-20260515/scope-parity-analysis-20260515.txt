ANALYSIS ADDENDUM: Scope Parity Position
Evidence Bundle: auth-convergence-client-20260515
Date: 2026-05-15
Event Spine Record:
  event_id:       1dae8d21-95fc-40a0-a8fd-8e4b7fe7ae63
  correlation_id: cd36e3a2-9d51-4304-893d-7463f323eb54
  published_at:   2026-05-15T08:45:44Z
  subject:        ev.ops.tenant-6f4f45b96f64
  event_type:     auth.convergence.analysis
================================================================================

PRIMARY REVISION TO PRIOR ANALYSIS
-----------------------------------

The blocker is no longer protocol capability.
It is authorization/scope parity.

This is a materially healthier position than the original 2026-04-29 framing,
which implied the auth-plane may require a full redesign. The current finding
is that the architectural path is fundamentally viable — only scope convergence
work remains on the server side.


CONFIRMED — kh-prod OIDC INFRASTRUCTURE IS VIABLE
---------------------------------------------------

kh-prod already supports (confirmed live 2026-05-15):

  PKCE-compatible OIDC ............. YES  (code_challenge_methods: ["plain","S256"])
  device_authorization_endpoint .... YES  (.../kh-prod/protocol/openid-connect/auth/device)
  device_code grant type ........... YES  (urn:ietf:params:oauth:grant-type:device_code)

Assumption invalidated:
  "keyhole-mcp realm was required for device flow infrastructure"
  This was the prior implicit assumption. It is no longer true.


ACTUAL REMAINING GAP — SCOPE PARITY
-------------------------------------

The remaining blocker is identity/claims/scope convergence. Specifically,
kh-prod currently lacks these scopes that keyhole-mcp exposes:

  Missing in kh-prod:
    builder
    cohort-worker-claims
    runtime:read
    runtime:write

  Also required (not yet verified):
    protocol mapper parity
    token claims parity
    audience parity
    role mapping parity
    per-client device flow enablement for keyhole-cli in kh-prod

This is a much narrower and safer fix than a full auth-plane redesign.


ARCHITECTURAL DOCTRINE ALIGNMENT
----------------------------------

This finding aligns with canonical doctrine:

  humans authenticate in kh-prod          (correct lane for user accounts)
  machine/runtime participation in keyhole-mcp (correct lane for service clients)
  SDK/CLI brokers human identities         (does not relocate humans to machine realm)

The convergence direction is doctrinally correct.


CLIENT PATCH RESTRAINT — DO NOT APPLY YET
-------------------------------------------

Flipping DEFAULT_AUTH_SERVER before scope parity exists would create
SILENT CAPABILITY REGRESSION. Specific failure modes:

  - builder workflows partially succeeding (scoped to partial token)
  - runtime actions failing later in the flow (claims missing)
  - hidden claim loss (no immediate error surface)
  - inconsistent whoami surfaces (server sees different identity shape)
  - broken downstream authorization assumptions (cascading tenant/org effects)

This would produce a very hard-to-debug split-identity condition.

The client patch (proposed-client-patch.diff) MUST NOT be applied until
the server backend returns proof of all scope/claims parity requirements.


OPERATIONAL DISCIPLINE — ISOLATION REQUIREMENT
------------------------------------------------

DO NOT merge the following into one operational window:

  auth convergence
  substrate stabilization
  RES-2

The cluster recently exited a major substrate repair cycle. Stacking:

  auth-plane mutation
  network-plane mutation
  enforcement-plane mutation

simultaneously creates compounded risk with shared blast radius.


EVIDENCE QUALITY CONFIRMATION
-------------------------------

All KEYHOLE_* environment variables were unset during evidence collection.
This means:

  - behavior was reproduced in canonical/default conditions
  - no hidden local overrides
  - no developer-environment contamination

This strengthens the evidence materiality.


CURRENT OPERATIONAL POSITION
------------------------------

STABLE:
  cluster substrate
  bridge enforcement
  VS Code remote auth (paul@keyholesolution.com / kh-prod)
  auth behavior reproducibility
  convergence evidence (this bundle)

BLOCKED:
  full auth default flip
  bridge retirement
  Felix-alone enforcement

NEXT HIGH-VALUE WORK (dependency chain order):
  1. RES-2 (Calico/ipset compatibility)
  2. kh-prod scope parity (builder, cohort-worker-claims, runtime:read, runtime:write)
  3. protocol mapper convergence
  4. controlled auth default flip (server proof received → client patch applied)


SERVER BACKEND REQUIREMENTS BEFORE FLIP (COMPLETE LIST)
---------------------------------------------------------

  1. Create or verify keyhole-cli client in kh-prod
  2. Enable Authorization Code + PKCE S256 for keyhole-cli in kh-prod
  3. Enable Device Authorization Grant for keyhole-cli in kh-prod
  4. Add missing scopes to kh-prod or map to keyhole-cli:
       builder, cohort-worker-claims, runtime:read, runtime:write
  5. Verify protocol mapper/claims parity with keyhole-mcp token shape
  6. Verify token audience parity
  7. Verify role mapping parity
  8. Confirm MCP /mcp/v1/whoami accepts kh-prod tokens from keyhole-cli
  9. Confirm whoami resolves paul@keyholesolution.com to correct
     tenant/org/cohort/worker context
 10. Return proof artifacts authorizing the SDK client default flip

Client-side change authorized ONLY after all 10 items are confirmed:
  DEFAULT_AUTH_SERVER -> https://auth.keyholesolution.com/realms/kh-prod
  DEFAULT_REALM       -> kh-prod
