# Optional governed server configuration.
# Local validation does not require these values.

KEYHOLE_MCP_URL=https://your-keyhole-server.example.com
KEYHOLE_MCP_TOKEN=replace_me

# Optional local tool state. Leave unset to use the default ignored .keyhole/ path.
KEYHOLE_HOME=
KEYHOLE_STATE_DIR=
